    const state = {
      userHasSeenIntro: false,
      userHasCompletedTodaysThing: true,
      userHasSharedWithFriends: false
    } 

      // userHasSeenIntro: true,
      // userHasCompletedTodaysThing: true,
      // userHasSharedWithFriends: false
      // expect(view).to(show the share dialog)
      // expect(view).to(show crossed off thing)
      // expect(nextnotification).not.tobe(today)
      // expect(nextnotification).to(be scheduled for tomorrow)

    // if the user hasn’t completed a thing today
    // things.getNewThing()

    // if the user hasn’t comepleted a thing today