import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  shareButton: {
    alignItems: "center",
    padding: 10,
    borderColor: "#ffffff",
    borderWidth: 2,
    borderRadius: 3,
    marginTop: 20,
  },
  shareButtonText: {
    fontSize: 24,
    color: "#ffffff",
  },
});